# Inisialisasi PostgreSQL

Direktori ini disediakan untuk berkas inisialisasi PostgreSQL yang perlu ikut
disimpan di repositori. Saat ini skema dan data awal dikelola oleh Prisma
melalui layanan `db-init` di `docker-compose.yml`, jadi belum ada skrip SQL
inisialisasi yang diperlukan.
